import 'package:flutter/material.dart';

class RouteInShellSecond extends StatelessWidget {
  const RouteInShellSecond({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('this is the route in shell - second one'),
    );
  }
}
