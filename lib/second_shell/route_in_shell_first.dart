import 'package:flutter/material.dart';

class RouteInShellFirst extends StatelessWidget {
  const RouteInShellFirst({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('this is the route in shell - first one'),
    );
  }
}
